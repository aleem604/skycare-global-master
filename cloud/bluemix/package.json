{
   "name": "send-emails",
   "main": "sendemails_action.js",
   "version": "0.1.0",
   "dependencies": {
      "@sendgrid/mail": "^6.3.1"
   },
   "scripts": {
       "deploy": "zip -rq action.zip sendemails_action.js package.json node_modules/ && bx wsk action update sendemails --kind nodejs:8 action.zip && rm action.zip"
   }
}
